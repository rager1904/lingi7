import React, { useState } from "react";
const notifications = ["Your order updates will appear here.", "Personalized drops and price alerts are enabled.", "We’ll only send messages that matter."];
const NotificationsPage: React.FC = () => { const [items, setItems] = useState(notifications); return <main className="mx-auto max-w-3xl px-4 py-10 sm:px-6"><div className="flex items-end justify-between"><div><p className="text-sm font-bold tracking-[.16em] text-blue-600">UPDATES</p><h1 className="mt-2 text-4xl font-black tracking-tight text-slate-950">Notifications.</h1></div><button onClick={() => setItems([])} className="text-sm font-bold text-blue-600">Mark all read</button></div><div className="mt-8 space-y-3">{items.length ? items.map((item) => <article key={item} className="flex gap-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><span className="grid h-10 w-10 place-items-center rounded-xl bg-blue-50 text-blue-600">✦</span><p className="pt-2 text-sm text-slate-700">{item}</p></article>) : <div className="rounded-2xl bg-slate-100 p-12 text-center text-sm text-slate-500">You’re all caught up.</div>}</div></main>; };
export default NotificationsPage;
