# ─────────────────────────────────────────────────────────────────────────────
# Lingi7 — Base Requirements (shared across all environments)
# Pin all versions explicitly. Update with care — test in staging first.
# ─────────────────────────────────────────────────────────────────────────────

# ── Django Core ───────────────────────────────────────────────────────────────
Django==4.2.16
djangorestframework==3.15.2
django-cors-headers==4.4.0
djangorestframework-simplejwt==5.3.1
drf-spectacular==0.29.0        # API schema generation & documentation
dj-database-url==2.2.0

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9        # Binary for dev; use psycopg2 (no -binary) in prod Dockerfile

# ── Cache & Task Queue ────────────────────────────────────────────────────────
redis==5.0.8
celery==5.4.0
django-celery-beat==2.7.0     # DB-backed beat scheduler — avoids schedule drift
django-celery-results==2.5.1
kombu==5.3.4                  # Celery messaging library

# ── Storage ───────────────────────────────────────────────────────────────────
boto3==1.35.0                 # AWS S3 / Cloudflare R2
django-storages==1.14.4       # Django storage backend for S3/R2
Pillow==10.4.0                # Image processing for product photos

# ── Auth & Security ───────────────────────────────────────────────────────────
django-otp==1.5.1             # 2FA TOTP for staff/admin accounts
qrcode==8.0                   # QR code generation for 2FA setup
cryptography==43.0.1          # Required by django-otp

# ── API Clients ──────────────────────────────────────────────────────────────
httpx==0.27.2                 # Async HTTP client for payment provider APIs

# ── Utilities ─────────────────────────────────────────────────────────────────
python-decouple==3.8          # Environment variable loading
python-dateutil==2.9.0
requests==2.32.3              # HTTP client for payment provider APIs
phonenumbers==8.13.45         # Zambian phone number validation (MTN/Airtel)
django-filter==24.3           # DRF filtering support
django-extensions==3.2.3      # shell_plus, show_urls, etc.

# ── ML (used in fraud pipeline at runtime) ────────────────────────────────────
scikit-learn==1.5.2
xgboost==2.1.1
joblib==1.4.2                 # Model serialisation
numpy==1.26.4
pandas==2.2.3
